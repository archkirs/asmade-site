(()=>{const buttons=[...document.querySelectorAll('[data-audience-step]')];const title=document.querySelector('[data-audience-title]');const copy=document.querySelector('[data-audience-copy]');if(buttons.length&&title&&copy){const content=JSON.parse(document.querySelector('[data-audience-content]')?.textContent||'{}');buttons.forEach(button=>button.addEventListener('click',()=>{const item=content[button.dataset.audienceStep];if(!item)return;buttons.forEach(b=>{const active=b===button;b.classList.toggle('is-active',active);b.setAttribute('aria-selected',active?'true':'false')});title.textContent=item.title;copy.textContent=item.copy}))}const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce){document.querySelectorAll('.fade').forEach(el=>el.classList.add('on'));return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('on');observer.unobserve(entry.target)}}),{threshold:.1});document.querySelectorAll('.fade').forEach(el=>observer.observe(el))})();